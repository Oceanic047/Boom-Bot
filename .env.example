# Discord Webhook URL for posting alerts
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR_WEBHOOK_ID/YOUR_WEBHOOK_TOKEN

# Pump.fun API endpoint (adjust if needed)
PUMPFUN_API_URL=https://frontend-api.pump.fun/coins

# Polling interval in seconds
POLL_INTERVAL=60

# Alert threshold (0-100)
ALERT_SCORE_THRESHOLD=50

# Thresholds for trend scoring
MIN_VOLUME_THRESHOLD=1000
MIN_LIQUIDITY_THRESHOLD=5000
MIN_HOLDER_COUNT=10
VOLUME_GROWTH_WEIGHT=0.4
LIQUIDITY_WEIGHT=0.3
HOLDER_WEIGHT=0.2
AGE_WEIGHT=0.1
